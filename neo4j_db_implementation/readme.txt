The script contains the Cypher code to implement the OpenFlights graph in Neo4j where the nodes represent airports and the edges represent flights. 

The two data files must first be attached to the database in order to run the Cypher query. 